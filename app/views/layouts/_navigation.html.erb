<%# navigation styled for Bootstrap 3.0 %>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to (image_tag "pepp_logo.png", width: 60 , height: "auto", alt: "Main"), root_path, class: 'navbar-brand' %>
      <%# asset_path('pepp_logo.png') %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <%= render 'layouts/navigation_links' %>
      </ul>
      <% if user_signed_in? %>
    <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
        <%=form_for users_path, method: :get do |f| %>
  
    <%= f.text_field :q , class: "form-control", placeholder: "Search" %>
    <%= f.submit "Submit", name: nil, class: "btn btn-default" %>
    </form>
 <% end %>
    </div>
  <% end %>
  </div>
</nav>
